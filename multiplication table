#include <stdio.h>

int main() {
    int n,ans,i;
    printf("enter the number to get the table: ");
    scanf("%d",&n);
    for(i=0;i<11;i++){
        ans=i*n;
        printf("%d * %d = %d\n",i,n,ans);
    }
    return 0;
}
