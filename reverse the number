#include <stdio.h>

int main()
{
    int n,reverse=0;
    printf("enter the number ");
    scanf("%d",&n);
    while(n!=0){
        reverse=reverse*10;
        reverse=reverse+n%10;
        n=n/10;
    }   
    printf("the reverse of the given number is %d ",reverse);
    return 0;
}
