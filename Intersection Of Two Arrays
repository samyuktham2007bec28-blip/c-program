#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
int* intersection(int* nums1, int nums1Size, int* nums2, int nums2Size, int* returnSize) {
    bool present[1001] = {false};
    for (int i = 0; i < nums1Size; i++) {
        present[nums1[i]] = true;
    }
    int maxIntersectionSize = nums1Size < nums2Size ? nums1Size : nums2Size;
    int* result = (int*)malloc(maxIntersectionSize * sizeof(int));
    *returnSize = 0;
    for (int i = 0; i < nums2Size; i++) {
        int currentNum = nums2[i];
        if (present[currentNum]) {
            result[(*returnSize)++] = currentNum;
            present[currentNum] = false; 
        }
    }
    return result;
}
